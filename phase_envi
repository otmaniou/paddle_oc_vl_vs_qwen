import inspect
import finegrained_fp8

print("=== SIGNATURES FINEGRAINED FP8 ===\n")

functions = [
    "fp8_act_quant",
    "w8a8_block_fp8_matmul",
    "w8a8_block_fp8_matmul_batched",
    "w8a8_block_fp8_matmul_grouped",
]

for name in functions:
    fn = getattr(finegrained_fp8, name)
    print(name)
    try:
        print("Signature :", inspect.signature(fn))
    except Exception as e:
        print("Signature non disponible :", e)

    print("Module    :", fn.__module__)
    print()
