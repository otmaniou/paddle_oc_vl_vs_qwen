python - <<'PY'
import sys

print("Python :", sys.version)
print("\n--- TEST FINEGRAINED FP8 ---")

try:
    import finegrained_fp8
    print("IMPORT finegrained_fp8 : OK")
    print("Chemin :", finegrained_fp8.__file__)

    print("\nContenu exposé :")
    print([x for x in dir(finegrained_fp8) if not x.startswith("_")])

except Exception as e:
    print("IMPORT finegrained_fp8 : ERREUR")
    print(type(e).__name__, ":", e)
PY
