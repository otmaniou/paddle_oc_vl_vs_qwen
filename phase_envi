echo "========== SYSTEM =========="
uname -a

echo
echo "========== PYTHON =========="
which python
python --version

echo
echo "========== PIP =========="
which pip
python -m pip --version

echo
echo "========== GPU / DRIVER / CUDA =========="
nvidia-smi

echo
echo "========== NVCC =========="
which nvcc || echo "nvcc absent"
nvcc --version 2>/dev/null || true

echo
echo "========== GCC =========="
gcc --version 2>/dev/null || echo "gcc absent"

echo
echo "========== CONDA =========="
echo "CONDA_PREFIX=$CONDA_PREFIX"

echo
echo "========== PACKAGES INITIAUX =========="
python -m pip list
