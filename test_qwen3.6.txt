test_path = INPUT_DIR / 'ALGAN_IBRAHIM_202510.PDF'
if not test_path.exists(): test_path = pdfs[0]
pages = pdf_to_pages(test_path)
rep = ask_single(PROMPT_UNIVERSEL, pages[0]['image'])
print(rep['text'][:2000])
