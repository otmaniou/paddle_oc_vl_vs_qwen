for tp in [INPUT_DIR / 'ALGAN_IBRAHIM_202510.PDF',
           INPUT_DIR / '02700731011128500104-202510.PDF']:
    if tp.exists():
        pages = pdf_to_pages(tp)
        rep = ask_single(PROMPT_UNIVERSEL, pages[0]['image'])
        print(tp.name, '→', rep['text'][:600], '\n')
