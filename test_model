import sys
import kernels

print("Python :", sys.version)
print("kernels :", kernels.__version__)

# Chercher le finegrained-fp8 actuellement monté par Domino
import glob

paths = glob.glob(
    "/domino/edv/modelhub/**/finegrained-fp8/**/build/torch-cuda",
    recursive=True
)

print("\n=== finegrained-fp8 disponibles ===")
for p in paths:
    print(p)

print("\n=== Test kernels / Transformers ===")

from transformers import AutoProcessor, AutoModelForMultimodalLM

print("AutoProcessor OK")
print("AutoModelForMultimodalLM OK")







import os
import glob

for base in paths:
    print("\n" + "=" * 100)
    print(base)

    for f in sorted(glob.glob(os.path.join(base, "**", "*.py"), recursive=True)):
        print(f)
